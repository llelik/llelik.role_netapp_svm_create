---
# defaults file for na-ontap-vserver-create
netapp_hostname: "{{ inventory_hostname }}"
netapp_username: "{{ hostvars[inventory_hostname] }}"
netapp_password: "{{ hostvars[inventory_hostname] }}"
validate_certs: false
vservers:
vserver_dns:
lifs: 
cifs:
nfs:
fcp:
gateway:
ontap_version: 140
