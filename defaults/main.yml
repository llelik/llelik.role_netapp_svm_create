---
# defaults file for na-ontap-vserver-create
netapp_hostname: "{{ hostvars['jamaica'].hostname_fqdn }}"
netapp_username: "{{ hostvars['jamaica'].netapp_username }}"
netapp_password: "{{ hostvars['jamaica'].netapp_password }}"
validate_certs: false
vservers:
vserver_dns:
lifs: 
cifs:
nfs:
fcp:
gateway:
ontap_version: 140
